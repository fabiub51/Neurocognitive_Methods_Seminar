echo "# Neurocognitive_Methods_Seminar" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/fabiub51/Neurocognitive_Methods_Seminar.git
git push -u origin main

% List of open inputs
nrun = X; % enter the number of runs here
jobfile = {'/Users/fabiusberner/Documents/MATLAB/auditory/jobs/realign_job.m'};
jobs = repmat(jobfile, 1, nrun);
inputs = cell(0, nrun);
for crun = 1:nrun
end
spm('defaults', 'FMRI');
spm_jobman('run', jobs, inputs{:});
